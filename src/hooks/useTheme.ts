const useTheme = () => {
  return {
    colors: {
      sky: {
        fog: "#EAF6F6",
      },
      common: {
        black: "#000000",
        white: "#FFFFFF",
      },
    },
  };
};

export default useTheme;
